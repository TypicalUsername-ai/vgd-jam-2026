[package]
name = "level_selector"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
ron = "0.12"
serde = { version = "1", features = ["derive"] }
state = { path = "../state" }
tracing = "0.1.44"
