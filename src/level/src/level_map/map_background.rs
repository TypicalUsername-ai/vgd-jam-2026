use super::LevelMapConfig;
use bevy::prelude::*;

/// sets up flat non-interactive background
pub(crate) fn setup_background(mut commands: Commands, level_config: Res<LevelMapConfig>) {
    todo!()
}
